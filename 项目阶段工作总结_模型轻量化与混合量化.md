# 安全帽检测模型轻量化、混合量化与论文实验阶段总结

> 更新时间：2026-08-31  
> 项目：SHWD 安全帽佩戴检测（`hat`、`person` 两类）  
> 工程：MobileNetV2-YOLOv5 + 混合精度量化 + 后续 FPGA 部署预研

---

## 1. 项目目标与总体路线

本项目面向施工/工业现场的安全帽佩戴检测。总体目标不是单纯提高检测精度，而是在保持安全帽小目标检测能力的前提下，降低参数量、计算量和模型存储量，为后续 FPGA 部署准备可用模型。

已形成的技术路线如下：

```text
SHWD 数据集冻结与统一训练协议
        ↓
YOLOv5s 基线复现
        ↓
MobileNetV2 替换 YOLOv5 Backbone + 宽度系数消融
        ↓
结构、蒸馏、剪枝候选筛选（保留有效，淘汰无稳定收益方案）
        ↓
FP32 ONNX 导出 + INT8 / INT4+INT8 PTQ
        ↓
QAT 恢复实验（未获测试集稳定收益）
        ↓
按层敏感度筛选 INT4 区域，形成 SG-MPQ 混合量化策略
        ↓
三随机种子重复性实验 + 后续外部数据/FPGA验证
```

当前阶段的核心结论：**W0.625 是精度—复杂度最均衡的基础模型；混合 INT4/INT8 量化是当前最有效的部署压缩手段；敏感度引导的 G2+G6 位宽分配可获得更小的模型尺寸，但并非精度提升方法。**

---

## 2. 数据集、工程与可复现性建设

### 2.1 数据集

- 数据集：SHWD 安全帽佩戴检测数据集。
- 类别：`hat`（安全帽）、`person`（人员）。
- 数据划分：训练集 5457 张、验证集 607 张、测试集 1517 张。
- 测试集实例数：24436，其中 `hat` 1878、`person` 22558。
- 固定配置文件：`SHWD_YOLO/shwd.yaml`，已补充 `test` 划分。

### 2.2 数据划分冻结

已添加数据清单冻结工具：

```text
yolov5-master/data/scripts/freeze_shwd_manifest.py
```

用途：将训练、验证、测试图像与标签列表固定，避免后续训练过程中因文件变化、数据混用导致实验不可复现。

### 2.3 Git 与工程管理

- 论文专用工作分支：`paper/model-improvement-2026`。
- 已将模型、训练超参数、蒸馏、剪枝、量化脚本等代码变更按功能提交到 GitHub。
- 已避免使用 `git add .`，防止将 `runs`、权重、缓存或无关临时文件误提交。
- 训练权重、ONNX 模型和结果图不直接提交 Git，而是归档至 `E:\experiment_M2Y5`。

---

## 3. 统一实验协议

为保证不同模型比较公平，论文阶段统一采用以下训练协议：

| 项目 | 设置 |
|---|---|
| 输入尺寸 | 640 × 640 |
| 训练轮数 | 200 epochs |
| Early stopping | patience = 50 |
| batch size | 8 |
| dataloader workers | 4 |
| 优化器 | SGD |
| 初始学习率 | 0.01（普通训练） |
| 超参数文件 | `data/hyps/hyp.paper_scratch.yaml` |
| 随机种子 | seed = 0、1、2 |
| 训练集 | 5457 张 |
| 验证集 | 607 张 |
| 最终测试集 | 1517 张 |

量化验证统一采用 ONNX Runtime、输入 640 × 640、batch size 1。需要特别说明：当前 ONNX Runtime GPU 对 INT4 QDQ 图的速度不代表 FPGA 推理延迟，不能作为 FPGA 性能结论。

---

## 4. 基础模型构建与宽度系数消融

### 4.1 基础结构

模型采用 YOLOv5 的 FPN、PAN 和 Detect Head，使用 MobileNetV2 倒残差块替换原 YOLOv5s 的 Backbone。这样保留 YOLOv5 的多尺度检测框架，同时降低骨干网络计算复杂度。

### 4.2 宽度系数消融结果（seed = 0）

| 模型 | 参数量 | GFLOPs | 验证 mAP50-95 | 测试 mAP50-95 | 结论 |
|---|---:|---:|---:|---:|---|
| YOLOv5s | 7.016 M | 15.8 | 0.609 | 0.601 | 精度参考模型 |
| MobileNetV2 W0.50 | 2.369 M | 6.4 | 0.588 | 0.578 | 最轻，但精度下降明显 |
| MobileNetV2 W0.625 | 3.685 M | 10.1 | 0.599 | 0.588 | **主要平衡模型** |
| MobileNetV2 W0.75 | 5.283 M | 14.0 | 0.602 | 0.596 | 精度较高，但复杂度接近 YOLOv5s |

结论：W0.625 相比 YOLOv5s 明显减少参数量和计算量，同时仅存在有限精度下降；相比 W0.50，检测精度更稳定；相比 W0.75，具有更清晰的轻量化收益。因此后续模型、量化、剪枝和重复性实验均以 W0.625 为主模型。

### 4.3 早期 P2 分支探索

曾尝试引入 P2 小目标检测分支。该模型参数量约 2.38 M、计算量约 6.7 GFLOPs。虽然小目标场景具有理论动机，但统一协议下未形成优于 W0.625/W0.75 的稳定结果，且增加了 FPGA 多尺度特征缓存与实现复杂度。因此未作为最终结构。

---

## 5. 已尝试的模型改进及结论

本阶段没有只保留正结果，而是记录并排除了未获得稳定收益的方案。这些结果可作为论文消融实验或方案选择依据。

### 5.1 跨尺度特征融合模块

| 方案 | 主要思想 | 验证 mAP50-95 | 结论 |
|---|---|---:|---|
| SGCF | 语义引导跨尺度融合 | 0.597 | 未超过 W0.625 基线 |
| P4→P3 Fusion | 向小目标 P3 引入 P4 语义 | 0.601 | 小幅变化，可能属于随机波动 |
| RGCF | 残差门控跨尺度融合 | 0.599 | 与基线持平 |

结论：在当前 SHWD 数据分布和 W0.625 模型上，额外跨尺度融合没有产生足够稳定的收益，同时增加部署复杂度；不作为最终模型结构创新。

### 5.2 注意力与损失函数

| 方案 | 作用 | 结果 | 结论 |
|---|---|---|---|
| QCA（量化友好坐标注意力） | P3 小目标方向注意力 | 验证 mAP50-95 = 0.592 | 低于基线，不保留 |
| WIoU | 替代 CIoU 框回归损失 | 50 epoch 筛选 mAP50-95 = 0.540 | 明显下降，不保留 |

结论：不能仅因其他论文使用注意力或 WIoU 就直接采用。当前数据和结构下，上述模块没有带来稳定改进。

### 5.3 知识蒸馏

教师模型为 YOLOv5s，学生模型为 W0.625 MobileNetV2-YOLOv5。

| 蒸馏方案 | 说明 | 验证 mAP50-95 | 结论 |
|---|---|---:|---|
| Vanilla KD | 通用检测头输出蒸馏 | 0.599 | 与基线持平 |
| SCCD | 尺度—类别—置信度蒸馏，强调 P3 和 hat 类 | 0.598 | 未获稳定收益 |

结论：蒸馏流程已实现并验证可运行，但在当前教师/学生差距和数据场景下没有必要作为最终方法。

### 5.4 稀疏训练与剪枝预研

已在 `train.py` 中加入 BatchNorm γ 的 L1 稀疏正则化，用于为结构化剪枝创造通道重要性差异。

| 方案 | 训练设置 | 验证 mAP50-95 | 结论 |
|---|---|---:|---|
| BN 稀疏训练 | 50 epochs | 0.599 | 精度未下降，但 γ 分离不足 |

结论：当前没有执行真正的通道删除，因此模型参数量和 GFLOPs 没有实际下降。不能将该步骤称为“完成剪枝”；后续若有明确 FPGA 通道约束，可再开展结构化剪枝与微调。

---

## 6. ONNX 导出与量化实验

### 6.1 量化目标

目标是为未来 FPGA 部署降低模型权重存储量，而不是在当前 RTX GPU + ONNX Runtime 环境中追求 INT4 推理加速。

最终采用的基础量化原则：

```text
Detect Head 和解码节点：FP32
Backbone / Neck 常规卷积：INT8
选择的低敏感宏观层：INT4
```

检测头保持 FP32 的原因：全模型 INT8 PTQ 曾出现输出塌陷（检测结果为 0），表明检测坐标与类别/置信度输出对统一激活量化范围敏感。

### 6.2 基础量化结果（W0.625, seed = 0）

| 模型 | 测试 mAP50-95 | 模型大小 | 相对 FP32 | 结论 |
|---|---:|---:|---:|---|
| FP32 ONNX | 0.587 | 14.71 MB | 参考 | 部署精度参考 |
| INT8 + Detect FP32 | 0.582 | 4.49 MB | -69.5% | 有效 INT8 基线 |
| 固定 G2 深层 INT4 + 其余 INT8 | 0.581 | 3.35 MB | -77.2% | 精度优先的混合量化点 |

结论：固定深层混合量化可将 ONNX 文件大幅压缩，测试 mAP50-95 仅相对 FP32 下降 0.006，证明 INT4/INT8 混合量化对该模型具有可行性。

### 6.3 QAT（量化感知微调）探索

实现内容：

- 训练时对层 6–10 模拟 INT4 权重量化；
- 对其他 Backbone/Neck 卷积模拟 INT8 权重量化；
- Detect Head 保持 FP32；
- 使用低学习率从 W0.625 已训练权重微调 200 epochs；
- 导出前关闭训练期假量化，再执行同一套 PTQ。

结果：

| 方案 | 验证 mAP50-95 | 测试 mAP50-95 |
|---|---:|---:|
| QAT 后 FP32 ONNX | 0.601 | 未单独测试 |
| QAT 后混合 INT4/INT8 | 0.594 | 0.580 |
| 原固定 G2 混合 INT4/INT8 | 0.589 | 0.581 |

结论：QAT 在验证集有恢复现象，但测试集没有稳定收益，不能作为最终精度恢复方法。该结果应如实作为负向消融保留。

---

## 7. SG-MPQ：敏感度引导混合精度量化

### 7.1 方法动机

此前固定将层 6–10 设为 INT4，仍属于经验选择。为形成更有依据的部署策略，进一步将 Backbone 和 Neck 分为六个宏观层组，逐组执行“该组 INT4、其他卷积 INT8、Detect Head FP32”的单组敏感度试验。

### 7.2 单组敏感度结果

| 组别 | INT4 宏观层 | 验证 mAP50-95 | 模型大小 | 判断 |
|---|---|---:|---:|---|
| 全 INT8 + Detect FP32 | 无 | 约 0.591 | 4.49 MB | 参考 |
| G1 | 0–5 | 0.574 | 4.43 MB | 浅层特征敏感，淘汰 |
| G2 | 6–10 | 0.589 | 3.35 MB | 低敏感、最大压缩收益，保留 |
| G3 | 11–14 | 0.590 | 4.41 MB | 低敏感但节省有限，可选 |
| G4 | 15–18 | 0.582 | 4.47 MB | P3 小目标路径敏感，淘汰 |
| G5 | 19–21 | 0.585 | 4.42 MB | 精度损失偏大，淘汰 |
| G6 | 22–24 | 0.589 | 4.17 MB | 低敏感且有额外压缩收益，保留 |

### 7.3 SG-MPQ 最终组合

根据“精度损失小、模型大小节省大”的原则，选择 G2 + G6 为 INT4 层：

```text
INT4：6, 7, 8, 9, 10, 22, 23, 24
INT8：其余 Backbone / Neck 卷积
FP32：Detect Head 与解码节点
```

| 方案 | 验证 mAP50-95 | 测试 mAP50-95 | ONNX 大小 |
|---|---:|---:|---:|
| 固定 G2 INT4 | 0.589 | 0.581 | 3.35 MB |
| **SG-MPQ（G2+G6）** | **0.588** | **0.580** | **3.02 MB** |

最终结论：SG-MPQ 相较固定 G2 进一步压缩 0.33 MB（约 9.9%），测试精度仅下降 0.001；相较 FP32 ONNX，模型大小下降约 79.5%，测试 mAP50-95 下降 0.007。

SG-MPQ 的定位应为：**面向存储受限部署的精度—模型大小折中策略**，不是“提高检测精度”的模型模块。

---

## 8. 三随机种子重复性实验

为避免单次训练的偶然性，已开始 W0.625 基线的三种子重复实验。

| 种子 | 验证 mAP50-95 | 测试 mAP50-95 | 状态 |
|---|---:|---:|---|
| seed = 0 | 0.599 | 0.588 | 已完成 |
| seed = 1 | 0.595 | 0.588 | 已完成 |
| seed = 2 | 0.595 | 待测试 | 已完成训练 |

目前 seed 0、1 的测试精度一致，说明基础模型具有较好稳定性。seed 2 完成测试后，应对 0、1、2 三个模型使用**同一固定 SG-MPQ 策略（G2+G6）**导出、量化并测试，最终报告平均值 ± 标准差。

---

## 9. 当前可用于汇报的阶段性成果

### 9.1 已形成的成果

1. 完成 SHWD 数据集固定划分、训练配置固定和 Git 版本管理。
2. 构建 MobileNetV2-YOLOv5 检测模型，并完成 W0.50、W0.625、W0.75 宽度消融。
3. 确定 W0.625 为主要轻量模型：3.685 M 参数、10.1 GFLOPs。
4. 实现蒸馏、稀疏训练、结构融合、注意力、WIoU 等候选机制，并通过对照实验筛除无稳定收益方案。
5. 实现 FP32、INT8、INT4+INT8 混合 PTQ；解决检测头全 INT8 输出塌陷问题。
6. 实现混合精度假量化 QAT 流程，并完成严格的测试集验证。
7. 形成敏感度引导的 SG-MPQ 位宽分配策略，得到 3.02 MB、mAP50-95 0.580 的更小部署模型。
8. 建立量化实验归档目录：`E:\experiment_M2Y5\04_quantization\sensitivity_guided_mixed_precision`。

### 9.2 当前不能夸大的内容

1. 目前没有能够稳定提高检测精度的结构创新模块。
2. QAT、蒸馏、注意力和 WIoU 不能写成最终有效提升方法。
3. BN 稀疏训练不等于已经完成结构化剪枝。
4. ONNX Runtime GPU 的 INT4 推理速度不能代表 FPGA 推理延迟。
5. SG-MPQ 目前是验证集敏感度筛选后得到的部署策略；后续仍需完成三随机种子与外部数据集验证，才能增强论文说服力。

---

## 10. 下一步工作计划

### 阶段 A：完成可重复性证据（优先级最高）

1. 测试 seed=2 的 FP32 `.pt` 模型。
2. 对 seed=1、seed=2 导出 FP32 ONNX。
3. 对三个 seed 均应用固定 SG-MPQ（G2+G6）规则：校准图像固定为训练集前 128 张，Detect Head FP32。
4. 分别测试 FP32 与 SG-MPQ ONNX，计算 mAP50、mAP50-95 的均值 ± 标准差。
5. 根据测试集结果确定最终部署点：
   - 精度优先：固定 G2，3.35 MB；
   - 存储优先：SG-MPQ，3.02 MB。

### 阶段 B：论文对比实验

1. 用统一协议补充 YOLOv5n、YOLOv5s 及必要轻量模型对照。
2. 统计参数量、GFLOPs、ONNX 大小、mAP50、mAP50-95。
3. 补充可视化：PR 曲线、混淆矩阵、典型漏检/误检案例。
4. 尽可能引入公开外部安全帽数据集进行验证，避免只在 SHWD 上得出结论。

### 阶段 C：面向 FPGA 的后续工作

1. 根据目标板卡资源确认 BRAM/DDR 容量、支持的 INT4/INT8 乘加结构与片上缓存约束。
2. 依据硬件约束在“固定 G2”与“SG-MPQ”间选择最终位宽配置。
3. 导出适配 FPGA 工具链的模型和权重格式。
4. 在板端测量真实 FPS、单帧延迟、功耗、BRAM/DSP/逻辑资源占用。
5. 将硬件实现作为后续大论文的系统级扩展。

---

## 11. 汇报时可使用的一句话总结

本阶段完成了 MobileNetV2-YOLOv5 安全帽检测模型的轻量化与可复现训练体系建设，确定 W0.625 为精度—复杂度平衡模型；在此基础上，提出并验证了面向存储受限部署的敏感度引导 INT4/INT8 混合量化策略，使 ONNX 模型从 14.71 MB 压缩至 3.02 MB，在 SHWD 测试集上保持 0.580 mAP50-95，为后续 FPGA 部署和论文实验提供了基础。
