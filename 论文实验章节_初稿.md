# 实验与结果分析章节初稿

> 本文档承接 `论文方法章节_初稿.md` 的第 3 章，建议作为论文第 4 章使用。所有数值均来自 `分支实验全量对照汇总.md` 与已保存的训练、测试和量化记录。

## 4 实验与结果分析

### 4.1 实验环境与实现细节

实验在 Windows 环境下完成，Python 版本为 3.11.15，PyTorch 版本为 2.11.0+cu130，CUDA 版本为 13.0，GPU 为 NVIDIA GeForce RTX 5060 Ti（8 GB）。模型基于 PyTorch 版 YOLOv5 工程实现，训练、验证和测试均使用同一份冻结的数据划分。主模型训练时的主要配置见表 1。

| 项目 | 设置 |
|---|---|
| 基础框架 | YOLOv5 + MobileNetV2 Backbone |
| 输入尺寸 | 640 × 640 |
| 最大训练轮数 | 200 |
| 优化器 | SGD |
| 初始学习率 | 0.01 |
| batch size | 8 |
| workers | 4 |
| Early stopping | patience = 50 |
| 数据增强 | Mosaic、MixUp、HSV、水平翻转与随机尺度变换 |
| 随机种子 | 0、1、2 |
| 原始 `.pt` 最终测试 | batch size = 4 |
| ONNX 量化模型测试 | batch size = 1，ONNX Runtime CUDA Execution Provider |

除特别声明外，模型从头训练，不加载预训练权重。对于模型结构和超参数的选择，均先依据验证集结果判断；最终测试集用于报告已确定模型的性能。由于 ONNX 量化模型采用 batch size 1，且出现 QDQ/Memcpy 开销，其运行时间不与 PyTorch 模型的 batch size 4 速度直接比较。

### 4.2 数据集与评价指标

实验采用 SHWD 安全帽佩戴检测数据集的 YOLO 格式版本。该数据集将“佩戴安全帽的头部”定义为 `hat` 正类，将“未佩戴安全帽的普通头部”定义为 `person` 负类；本文检测的是头部安全帽佩戴状态，而非完整人体。训练集、验证集和测试集分别包含 5457、607 和 1517 张图像。测试集包含 24436 个标注实例，其中 `hat` 为 1878 个，`person` 为 22558 个。类别实例数存在明显不均衡，因此除总体指标外，正式定稿时建议补充 `hat` 类 AP、Precision 和 Recall，以支持对正类安全帽佩戴检测能力的针对性分析。

本文使用 Precision（P）、Recall（R）、mAP50 和 mAP50--95 评价检测性能。其中，mAP50 表示 IoU 阈值为 0.50 时的平均精度；mAP50--95 表示 IoU 阈值从 0.50 至 0.95、间隔为 0.05 时平均精度的均值，是本文的主要精度指标。模型参数量和 GFLOPs 用于衡量模型复杂度；ONNX 文件大小用于比较部署存储开销。

### 4.3 宽度系数消融与主模型选择

为选择轻量化 Backbone 的合适宽度，本文比较 YOLOv5s、MobileNetV2-YOLOv5 W0.50、W0.625 和 W0.75。该宽度消融使用相同的数据划分、输入尺寸和训练协议，结果如表 2 所示。

| 模型 | 参数量 (M) | GFLOPs | 验证集 mAP50--95 | 测试集 mAP50--95 |
|---|---:|---:|---:|---:|
| YOLOv5s | 7.016 | 15.8 | 0.609 | 0.601 |
| MobileNetV2-YOLOv5 W0.50 | 2.369 | 6.4 | 0.588 | 0.578 |
| **MobileNetV2-YOLOv5 W0.625** | **3.685** | **10.1** | **0.599** | **0.588** |
| MobileNetV2-YOLOv5 W0.75 | 5.283 | 14.0 | 0.602 | 0.596 |

W0.50 以最小的模型复杂度获得 0.578 的测试 mAP50--95，但相较其他宽度配置存在较明显精度损失。W0.75 的测试 mAP50--95 可达 0.596，但其 GFLOPs 增至 14.0，已接近 YOLOv5s 的 15.8。W0.625 在精度与复杂度之间表现出更好的折中：相较 YOLOv5s，参数量由 7.016M 降至 3.685M，减少 47.5%；GFLOPs 由 15.8 降至 10.1，减少 36.1%；测试 mAP50--95 仅下降 0.013。因此，后续结构消融、重复性实验与量化实验均以 W0.625 为主模型。

### 4.4 三随机种子稳定性验证

单次训练结果可能受到初始化、数据打乱和在线增强随机性的影响。为检验 W0.625 结论的稳定性，本文在相同数据、模型结构与训练超参数下使用三个随机种子重新训练并在测试集上评估，结果如表 3 所示。

| seed | Precision | Recall | mAP50 | mAP50--95 |
|---:|---:|---:|---:|---:|
| 0 | 0.921 | 0.882 | 0.932 | 0.588 |
| 1 | 0.919 | 0.880 | 0.931 | 0.588 |
| 2 | 0.923 | 0.875 | 0.931 | 0.591 |
| **平均 ± 样本标准差** | — | — | **0.931 ± 0.001** | **0.589 ± 0.002** |

三个 seed 的 mAP50--95 最大差异为 0.003，样本标准差为 0.002；mAP50 的样本标准差为 0.001。这说明 W0.625 在当前实验协议下具有较好的可重复性。后续论文中应报告 **0.589 ± 0.002**，而不只报告 seed=0 的单次测试结果。

### 4.5 结构模块与训练策略消融

为探索安全帽小目标检测的潜在改进方向，本文以 W0.625 为基础，尝试了跨尺度特征融合、注意力、知识蒸馏、边界框损失、BN 稀疏训练和量化感知训练等方案。完整的 200 epoch 验证结果如表 4 所示；仅运行 1 epoch 的冒烟实验不纳入性能比较。

| 方案 | 参数量 / GFLOPs | 验证集 mAP50--95 | 结果与处理 |
|---|---|---:|---|
| W0.625 原模型 | 3.685M / 10.1 | 0.599 | 参照模型 |
| SGCF 语义引导跨尺度融合 | 3.718M / 10.4 | 0.597 | 未提升，不采用 |
| Plain P4→P3 Fusion | 3.733M / 10.6 | 0.601 | 提升 0.002，接近随机波动，不作为核心方法 |
| RGCF 残差门控跨尺度融合 | 3.718M / 10.4 | 0.599 | 无稳定收益，不采用 |
| QCA 量化友好坐标注意力 | 3.687M / 10.2 | 0.592 | 精度下降，不采用 |
| 普通知识蒸馏 KD | 3.685M / 10.1 | 0.599 | 无稳定收益，不采用 |
| SCCD 蒸馏 | 3.685M / 10.1 | 0.598 | 无稳定收益，不采用 |
| WIoU | 3.685M / 10.1 | 0.540（50 epoch） | 明显下降，停止 |
| BN 稀疏正则 | 3.685M / 10.1 | 0.599（50 epoch） | 未实际删除通道，不称为结构化剪枝 |
| Mixed-QAT | 日志 FLOPs 约 7.0 | 0.601 | 后续部署精度无稳定优势，不采用 |

表 4 表明，简单叠加模块并不必然适合当前数据分布与轻量化网络。Plain P4→P3 Fusion 虽有 0.002 的验证集变化，但小于三 seed 主模型的随机波动量级，因而不足以支持其作为有效改进。QCA、两类蒸馏和跨尺度融合均未带来稳定收益；WIoU 在 50 epoch 筛选阶段出现明显下降。因此，本文不将上述模块堆叠到最终模型，而是保留结构更规整、复杂度更低的 W0.625 配置。

### 4.6 ONNX 导出与混合精度量化结果

首先验证 `.pt` 到 FP32 ONNX 的导出一致性，结果如表 5。三个 seed 的 ONNX 测试 mAP50--95 与对应 `.pt` 模型最多相差 0.001，说明导出过程基本不改变检测精度，FP32 ONNX 可作为量化对照起点。

| seed | `.pt` 测试 mAP50--95 | FP32 ONNX 测试 mAP50--95 | 差异 |
|---:|---:|---:|---:|
| 0 | 0.588 | 0.587 | -0.001 |
| 1 | 0.588 | 0.588 | 0.000 |
| 2 | 0.591 | 0.590 | -0.001 |

为评估混合精度量化，先构建保护 Detect/Decode 节点的 INT8 对照模型，再依据验证集敏感度选择 INT4 卷积层组。表 6 给出三随机种子上的测试结果。混合量化的“额外损失”定义为 INT8 对照 mAP50--95 与混合 INT4/INT8 mAP50--95 之差。

| seed | FP32 ONNX | INT8 对照 | 验证集筛选的 INT4 层组 | 混合 INT4/INT8 | INT8→混合额外损失 |
|---:|---:|---:|---|---:|---:|
| 0 | 0.587 | 0.582 | G2+G6 | 0.580 | 0.002 |
| 1 | 0.588 | 0.575 | G2+G3 | 0.574 | 0.001 |
| 2 | 0.590 | 0.567 | G2-only | 0.565 | 0.002 |
| **平均 ± 样本标准差** | **0.588 ± 0.002** | **0.575 ± 0.008** | 模型自适应 | **0.573 ± 0.008** | **0.002** |

结果表明，INT8 对照相较 FP32 ONNX 存在一定精度损失；在此基础上，经过验证集敏感度筛选的混合 INT4/INT8 配置平均仅额外损失 0.002 mAP50--95。以 seed=0 为例，FP32 ONNX 文件由 14.7 MB 缩小至 4.49 MB 的 INT8 对照模型；G2+G6 混合配置进一步缩小至 3.02 MB。该结果验证了选择性降低卷积权重位宽的存储压缩价值。

不同 seed 对应的低敏感层组并不完全一致，说明固定使用一组经验层号并不可靠。本文因此采用验证集驱动的模型特异性层组筛选，而不宣称某个固定的 INT4 层组适用于所有已训练权重。需要特别说明，seed=2 的 G2-only 测试是在观察到 G2+G3 组合测试偏低后追加的补充验证；正式论文中应如实标注其探索性，避免将其表述为完全独立的最终模型选择。

### 4.7 定性结果与讨论

建议在本节加入图 4--图 6：W0.625 的训练收敛曲线、测试集 PR 曲线/混淆矩阵，以及安全帽小目标、遮挡和复杂背景下的预测可视化。可优先使用以下已有文件：

| 图件 | 推荐文件 |
|---|---|
| 训练曲线 | `runs/paper/mobilenetv2_w0625_s0/results.png` |
| 测试集 PR 曲线 | `runs/paper_val/mobilenetv2_w0625_s0_test_clean/PR_curve.png` |
| 测试集混淆矩阵 | `runs/paper_val/mobilenetv2_w0625_s0_test_clean/confusion_matrix.png` |
| 预测示例 | `runs/paper_val/mobilenetv2_w0625_s0_test_clean/val_batch*_pred.jpg` |

从复杂度、稳定性和量化结果综合分析，W0.625 是当前最适合作为后续边缘部署候选的模型：它显著低于 YOLOv5s 的参数量和计算量，三 seed 精度稳定，并可在保护输出节点的前提下进行混合低比特权重量化。另一方面，本文仍存在两项边界：第一，实验主要在 SHWD 单一数据集上完成，尚未进行独立外部数据集或真实施工视频的系统性验证；第二，当前 ONNX Runtime CUDA 上的 INT4/QDQ 图存在大量 Memcpy 开销，单图延迟约为 100--380 ms，不能作为 GPU 或 FPGA 的性能结论。后续应在确定的 FPGA 或嵌入式硬件平台上重新完成编译、资源占用、功耗和端到端延迟测量。

### 4.8 本章小结

本章在统一训练协议下验证了 MobileNetV2-YOLOv5 W0.625 的精度—复杂度折中优势。该模型以 3.685M 参数和 10.1 GFLOPs 在 SHWD 测试集上达到 0.589 ± 0.002 的 mAP50--95。多种跨尺度融合、注意力、蒸馏、损失函数与量化感知训练策略均未获得稳定收益，最终未被纳入模型。基于验证集敏感度的混合 INT4/INT8 量化在 INT8 对照基础上平均仅产生 0.002 mAP50--95 的额外损失，并为后续 FPGA 位宽分配和模型存储压缩提供了部署候选。

## 定稿前检查清单

- [ ] 从最终 `val.py` 输出补入 `hat`、`person` 两类的 AP、P、R；
- [ ] 检查图 4--图 6 是否有清晰的中文/英文坐标、图例和图注；
- [ ] 依据目标期刊格式将表 1--表 6 统一为三线表；
- [ ] 补充 SHWD 的公开来源、许可证与标签映射说明；
- [ ] 若补做外部数据/真实视频验证，在 4.7 节之后新增独立小节，不与 SHWD 的 mAP 横向混合比较。
