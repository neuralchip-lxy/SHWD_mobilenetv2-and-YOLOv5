# 安全帽检测论文项目：新对话交接说明

> 更新日期：2026-09-04  
> 用途：在新对话中直接上传/提供本文件，使后续助手理解项目状态，避免重复训练、重复量化或误判实验结果。

## 1. 项目目标

面向安全帽场景，完成一个可写论文的轻量化检测模型，并为未来 FPGA 部署准备模型压缩/位宽分配实验。当前阶段的重点是：

1. 在 SHWD 数据集上获得可靠的轻量化检测结果；
2. 通过三随机种子证明结果稳定；
3. 探索敏感度引导的混合 INT4/INT8 量化；
4. **当前不宣称 FPGA 或 GPU INT4 已加速**，因为尚未有 FPGA 实测。

## 2. 工程与数据位置

```text
外层工作目录：
E:\codex_work\experiment of mobilenet2 and YOLO5

Git 项目：
E:\codex_work\experiment of mobilenet2 and YOLO5\yolov5-paper

YOLOv5 根目录（运行 train.py / val.py / export.py 的位置）：
E:\codex_work\experiment of mobilenet2 and YOLO5\yolov5-paper\yolov5-master

Git 分支：
paper/model-improvement-2026

数据集：
E:\codex_work\experiment of mobilenet2 and YOLO5\SHWD_YOLO

数据 YAML（相对 YOLO 根目录）：
../SHWD_YOLO/shwd.yaml
```

Python 环境：

```text
C:\Users\liuji\.conda\envs\pyenv\python.exe
```

数据划分：

| 集合 | 图像数 | 备注 |
|---|---:|---|
| train | 5457 | 训练 |
| val | 607 | 9925 instances，用于模型与层组筛选 |
| test | 1517 | 24436 instances，用于最终报告 |

测试集类别实例：`hat=1878`，`person=22558`。类别明显不均衡，但当前任务仍可训练。

## 3. 已选主模型与主结论

主模型：`YOLOv5s + MobileNetV2 Backbone, width=0.625`。

模型 YAML：

```text
models/yolov5s_mobilenetv2_w0625.yaml
```

它不是只改一个宽度数字：MobileNetV2 Backbone 使用倒残差和深度可分离卷积；模型日志为 169 layers，而原 YOLOv5s 为 157 layers。

### 3.1 宽度消融（已完成）

| 模型 | 参数量 | GFLOPs | val mAP50-95 | test mAP50-95 |
|---|---:|---:|---:|---:|
| YOLOv5s | 7.016M | 15.8 | 0.609 | 0.601 |
| MobileNetV2-W0.50 | 2.369M | 6.4 | 0.588 | 0.578 |
| **MobileNetV2-W0.625** | **3.685M** | **10.1** | **0.599** | **0.588**（seed 0） |
| MobileNetV2-W0.75 | 5.283M | 14.0 | 0.602 | 0.596 |

W0.625 是当前精度—复杂度折中点：相对 YOLOv5s 参数减少约 47.5%，测试 mAP50-95 下降 0.013。

### 3.2 三随机种子（已完成）

均为原始 FP32 `.pt`，同一数据/模型/超参数，test batch=4：

| seed | Precision | Recall | mAP50 | mAP50-95 |
|---:|---:|---:|---:|---:|
| 0 | 0.921 | 0.882 | 0.932 | 0.588 |
| 1 | 0.919 | 0.880 | 0.931 | 0.588 |
| 2 | 0.923 | 0.875 | 0.931 | 0.591 |
| 平均 ± 样本标准差 | — | — | **0.931 ± 0.001** | **0.589 ± 0.002** |

主论文精度结论应使用：**mAP50-95 = 0.589 ± 0.002**。

权重目录：

```text
runs/paper/mobilenetv2_w0625_s0/weights/best.pt
runs/paper/mobilenetv2_w0625_s1/weights/best.pt
runs/paper/mobilenetv2_w0625_s2/weights/best.pt
```

## 4. 已做但未采用的模型改动

这些方案均保留为消融/负结果，不应虚构为最终创新：

| 方案 | val mAP50-95 | 结论 |
|---|---:|---|
| SGCF 语义引导跨尺度融合 | 0.597 | 未提升 |
| Plain P4→P3 Fusion | 0.601 | +0.002，接近随机波动，不作为核心结论 |
| RGCF 残差门控跨尺度融合 | 0.599 | 无稳定提升 |
| QCA 坐标注意力 | 0.592 | 下降 |
| 普通 KD | 0.599 | 无提升 |
| SCCD 蒸馏 | 0.598 | 无提升 |
| WIoU（50 epochs） | 0.540 | 明显下降，停止 |
| BN 稀疏正则（50 epochs） | 0.599 | 未删除实际通道，不能称结构化剪枝 |
| Mixed-QAT | 训练 val 0.601；后续部署测试约 0.580 | 没有稳定部署优势 |

所有 1 epoch 冒烟实验只证明代码可运行，不能用于性能对比。

## 5. 量化工作：结论必须准确表述

### 5.1 当前量化脚本

```text
data/scripts/quantize_onnx_int8.py
```

主要参数：

```text
--model                 输入 FP32 ONNX
--calibration-dir       训练图像目录
--calibration-images    校准图数量（本实验统一主要用 128）
--protect-detect        保留检测头/Decode 相关 40 个节点 FP32
--int4-layers           指定改为 INT4 的宏层索引；其余可量化卷积为 INT8
```

量化流程：

```text
best.pt → best.onnx（FP32）
        → INT8 对照 ONNX（protect Detect）
        → 候选层组改为 INT4 的混合 ONNX
```

严格敏感度定义：

```text
层组额外损失 = mAP50-95(INT8 对照组) - mAP50-95(该层组 INT4)
```

不要将 INT4 模型直接与 FP32 比较后就声称“某层敏感”；应先与同 seed 的 INT8 对照组比较。

### 5.2 seed=0 初始量化结果

| 方案 | 文件大小 | test mAP50-95 |
|---|---:|---:|
| FP32 ONNX | 14.7 MB | 0.587 |
| INT8，对 Detect 保护 | 4.49 MB | 0.582 |
| G2 INT4 + 其余 INT8 | 3.35 MB | 0.581 |
| G2+G6 INT4 + 其余 INT8 | 3.02 MB | 0.580 |

最初的全 INT8（未保护输出）出现 all=0，已废弃。

### 5.3 seed=1 严格量化对照（已完成）

| 方案 | val mAP50-95 | test mAP50-95 | 相对 INT8 的测试额外损失 |
|---|---:|---:|---:|
| FP32 ONNX | — | 0.588 | — |
| INT8 对照 | 0.583 | 0.575 | 0.000 |
| G2 INT4 | 0.583 | 0.573 | 0.002 |
| G3 INT4 | 0.584 | 0.575 | 0.000 |
| **G2+G3 INT4** | **0.582** | **0.574** | **0.001** |

seed=1 可选 G2+G3（20 个 Conv 权重 INT4）。

### 5.4 seed=2 严格量化对照（已完成）

| 方案 | val mAP50-95 | test mAP50-95 | 相对 INT8 的测试额外损失 |
|---|---:|---:|---:|
| FP32 ONNX | — | 0.590 | — |
| INT8 对照 | 0.576 | 0.567 | 0.000 |
| G2 INT4 | 0.574 | 0.565 | 0.002 |
| G3 INT4 | 0.573 | 未单独测试 | — |
| G2+G3 INT4 | 0.573 | 0.563 | 0.004 |

seed=2 中 G3 位于边界，G2+G3 的组合误差偏大；G2-only 的补充测试更稳。

### 5.5 量化总览与正确结论

| seed | FP32 ONNX test | INT8 test | 当前混合 INT4/INT8 test | 当前选择 |
|---:|---:|---:|---:|---|
| 0 | 0.587 | 0.582 | 0.580 | G2+G6 |
| 1 | 0.588 | 0.575 | 0.574 | G2+G3 |
| 2 | 0.590 | 0.567 | 0.565 | G2-only（补充验证） |
| 平均 | 0.588 | 0.575 | 0.573 | 自适应层组 |

可以写：

1. 不同 seed / 不同权重的层组 INT4 敏感度不同；
2. 固定使用同一批 INT4 层不可靠；
3. 基于验证集选择低敏感层后，INT8→混合 INT4 平均额外损失约 0.002 mAP50-95；
4. 结果为 FPGA 后续的位宽分配提供候选。

不可以写：

1. 量化提高了检测精度；
2. 当前 INT4 ONNX Runtime GPU 推理更快；
3. 已在 FPGA 上实现加速。

当前 ONNX Runtime CUDA 的 INT4/QDQ 图出现大量 Memcpy 节点，单图推理约 100–380 ms，远慢于 FP32 ONNX 的约 6–9 ms。这是运行时/算子支持问题，不是 FPGA 实际速度。

**严谨性注记**：seed=2 的 G2-only 测试是在观察到 G2+G3 测试偏低后追加，因此论文中需标注为“补充消融验证”，不可叙述为完全独立的最终选择。

## 6. 运行与 Git 规则

### 6.1 运行方式

- 训练和断点续训：保留 PyCharm 运行配置；
- ONNX 导出、量化、val/test：直接在 PowerShell 终端执行；
- 运行 `train.py` / `val.py` 前当前目录必须是 `yolov5-master`。

常用终端开头：

```powershell
cd "E:\codex_work\experiment of mobilenet2 and YOLO5\yolov5-paper\yolov5-master"
& "C:\Users\liuji\.conda\envs\pyenv\python.exe" val.py ...
```

### 6.2 Git

只提交源码、模型 YAML、量化脚本、说明 Markdown。不要提交：

```text
runs/
*.pt
*.onnx
缓存文件、训练可视化图片
```

不要使用 `git add .`；应指定需要提交的源码文件。生成权重、ONNX、验证结果不需要 Git 提交。

## 7. 已知警告

| 警告 | 含义 | 是否影响当前精度 |
|---|---|---|
| `Cache directory ... not writeable: WinError 183` | label cache 写入失败/冲突 | 不影响本次读取、训练或 mAP |
| `NMS time limit exceeded` | 后处理偶尔慢 | 指标可用，但速度统计不适合做硬件结论 |
| ONNX Runtime `Memcpy nodes are added` | 量化 QDQ 图数据传输/算子不充分支持 | 精度可用；当前延迟不可当 FPGA/GPU 加速结果 |

## 8. 现有文档与归档

完整全量对照表：

```text
E:\codex_work\experiment of mobilenet2 and YOLO5\yolov5-paper\分支实验全量对照汇总.md
```

此前阶段总结：

```text
E:\codex_work\experiment of mobilenet2 and YOLO5\yolov5-paper\项目阶段工作总结_模型轻量化与混合量化.md
```

量化外部归档：

```text
E:\experiment_M2Y5\04_quantization\sensitivity_guided_mixed_precision
```

## 9. 建议的新对话起点

当前实验已完成，**不要再无目的地训练或重复量化**。新对话建议优先做：

1. 将本说明和“分支实验全量对照汇总.md”转化为论文实验章节、答辩表格和图；
2. 整理 W0.625 主模型的 `results.png`、PR/F1 曲线、混淆矩阵和预测可视化；
3. 若老师要求真实场景验证，再建立小型视频外部测试集：保留 SHWD 为主训练集，补充视频抽帧作为外部测试，不要破坏现有划分；
4. 若获得 FPGA/Jetson 硬件，再按实际工具链做 INT8/INT4 编译和 FPS、延迟、功耗、资源占用测试。

## 10. 术语速记

| 术语 | 简明解释 |
|---|---|
| seed | 控制初始化、样本打乱、增强等随机过程；0/1/2 用于验证稳定性 |
| mAP50 | IoU≥0.50 时的检测精度，较宽松 |
| mAP50-95 | IoU 0.50 至 0.95 每隔 0.05 求 AP 后平均，更严格，是主指标 |
| FPS | 每秒处理图像帧数，约等于 `1000 / 单帧总耗时(ms)` |
| 视频流检测 | 连续处理摄像头帧 |
| 抽帧检测 | 每隔若干帧/每秒取若干帧进行检测，降低算力需求 |
| TensorRT | NVIDIA GPU 的推理加速工具，不是 FPGA 工具链 |
| Jetson Orin Nano | NVIDIA 嵌入式 GPU 平台，不是 FPGA |
