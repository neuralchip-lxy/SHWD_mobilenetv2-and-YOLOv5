# `paper/model-improvement-2026` 分支实验全量对照汇总

> 更新时间：2026-09-03  
> 目的：汇总从创建 `paper/model-improvement-2026` 分支以来完成的模型、训练、验证、测试、量化与部署探索。  
> 说明：表中 `mAP50-95` 均为 `all` 类指标；除特别说明外，训练验证使用 SHWD 数据集、输入尺寸 640。训练集/验证集/测试集分别为 5457 / 607 / 1517 张图像。测试集含 24436 个实例（hat 1878，person 22558）。

## 1. 固定实验协议

| 项目 | 统一设置 |
|---|---|
| 数据配置 | `../SHWD_YOLO/shwd.yaml`，包含 `train`、`val`、`test` |
| 训练图像尺寸 | 640 × 640 |
| 常规训练 | 200 epochs，batch-size 8，workers 4，device 0，patience 50 |
| 超参数 | `data/hyps/hyp.paper_scratch.yaml` |
| 主随机种子 | 0、1、2 |
| 原始 PT 测试 | batch-size 4，workers 4 |
| ONNX/量化模型测试 | batch-size 1，workers 0，ONNX Runtime CUDA EP |
| 重要原则 | 验证集用于筛选模型/量化层组；测试集用于最终报告。当前历史实验中个别量化补充试验在看到测试结果后追加，已在相应表格中说明。 |

## 2. 基线与宽度系数消融

### 2.1 主模型对照

| 编号 | 模型 | 参数量 | GFLOPs | 验证集 mAP50-95 | 测试集 mAP50-95 | 结论 |
|---|---|---:|---:|---:|---:|---|
| B0 | YOLOv5s 原始基线 | 7.016M | 15.8 | 0.609 | 0.601 | 精度基线 |
| B1 | MobileNetV2-W0.50 | 2.369M | 6.4 | 0.588 | 0.578 | 最轻，但精度下降较明显 |
| B2 | MobileNetV2-W0.625（seed=0） | 3.685M | 10.1 | 0.599 | 0.588 | **主轻量化模型** |
| B3 | MobileNetV2-W0.75 | 5.283M | 14.0 | 0.602 | 0.596 | 精度接近 YOLOv5s，但复杂度回升 |

### 2.2 宽度选择结论

相对 YOLOv5s，W0.625：

- 参数量由 7.016M 降至 3.685M，减少约 **47.5%**；
- GFLOPs 由 15.8 降至 10.1，减少约 **36.1%**；
- 测试 mAP50-95 由 0.601 降至 0.588，下降 **0.013**；
- W0.50 更轻但精度偏低；W0.75 仅比 W0.625 高 0.008 mAP50-95，却接近 YOLOv5s 的计算量。

因此选取 **MobileNetV2-W0.625** 作为精度—复杂度折中模型。该模型并非只修改普通 YOLOv5 的一个数值：它使用 MobileNetV2 倒残差/深度可分离卷积 Backbone；日志层数为 169，而 YOLOv5s 为 157。

## 3. W0.625 三随机种子复现

### 3.1 原始 PyTorch `.pt` 最终测试

| seed | Precision | Recall | mAP50 | mAP50-95 | 推理 | 备注 |
|---:|---:|---:|---:|---:|---|---|
| 0 | 0.921 | 0.882 | 0.932 | 0.588 | 3.5 ms 推理 + 1.2 ms NMS/图（batch 4） | 主模型初始结果 |
| 1 | 0.919 | 0.880 | 0.931 | 0.588 | 5.3 ms 推理 + 2.3 ms NMS/图（batch 4） | 复现 |
| 2 | 0.923 | 0.875 | 0.931 | 0.591 | 4.4 ms 推理 + 1.8 ms NMS/图（batch 4） | 复现 |
| 平均 ± 样本标准差 | — | — | **0.931 ± 0.001** | **0.589 ± 0.002** | — | 论文主结果 |

> 三个 seed 的数据集、模型 YAML、训练 epochs 与超参数保持一致；seed 仅改变初始化、样本打乱与增强等随机过程。

### 3.2 FP32 ONNX 导出一致性

| seed | FP32 ONNX 测试 mAP50-95 | 与 PT 测试差异 | ONNX 单图推理 |
|---:|---:|---:|---:|
| 0 | 0.587 | -0.001 | 6.3 ms |
| 1 | 0.588 | 0.000 | 8.5 ms |
| 2 | 0.590 | -0.001 | 6.4 ms |

结论：`.pt → FP32 ONNX` 导出基本不改变检测精度；FP32 ONNX 是量化对照起点。

## 4. 结构模块与训练策略消融

> 下表均以 W0.625 为基础。带有“未采用”的结果不是失败数据，应保留为消融证据；它们表明简单叠加模块并不必然有效。

| 编号 | 方案 | 参数量 / GFLOPs | 训练设置 | 验证集 mAP50-95 | 结果与处理 |
|---|---|---|---:|---:|---|
| A0 | W0.625 原模型 | 3.685M / 10.1 | 200 epochs | 0.599 | 参照 |
| A1 | SGCF：语义引导跨尺度融合 | 3.718M / 10.4 | 200 epochs | 0.597 | 未提升，未采用 |
| A2 | Plain P4→P3 Fusion | 3.733M / 10.6 | 200 epochs | 0.601 | +0.002，幅度接近随机波动，不作为核心结论 |
| A3 | RGCF：残差门控跨尺度融合 | 3.718M / 10.4 | 200 epochs | 0.599 | 无稳定提升，未采用 |
| A4 | QCA：量化友好坐标注意力 | 3.687M / 10.2 | 200 epochs | 0.592 | 下降，未采用 |
| A5 | WIoU 筛选试验 | 3.685M / 10.1 | 50 epochs | 0.540 | 显著下降，停止 |
| A6 | 普通知识蒸馏 KD | 3.685M / 10.1 | 200 epochs | 0.599 | 无提升，未采用 |
| A7 | SCCD：尺度—类别—置信度蒸馏 | 3.685M / 10.1 | 200 epochs | 0.598 | 无提升，未采用 |
| A8 | BN 稀疏正则 | 3.685M / 10.1 | 50 epochs | 0.599 | 只得到稀疏训练，**未真正删除通道**，不称为结构化剪枝 |
| A9 | QAT fake-quant 训练 | 日志 FLOPs 约 7.0 | 200 epochs | 0.601 | 训练验证略高，但后续部署精度无稳定优势 |

### 4.1 冒烟实验（只验证代码能否跑通）

以下均为 1 epoch，不可与 200 epoch 结果比较，也不进入论文主性能表：

| 冒烟方案 | 结果 |
|---|---|
| SGCF、P4Fusion、RGCF、普通 KD、SCCD | 成功完成一个 epoch，说明结构/蒸馏代码可执行 |
| WIoU、BN 稀疏、Mixed-QAT | 成功完成一个 epoch，说明损失/伪量化代码可执行 |
| 所有 1 epoch 的极低 mAP | 正常现象：模型尚未收敛，不代表方法效果 |

## 5. 量化与部署探索

### 5.1 量化术语与边界

| 名称 | 含义 |
|---|---|
| FP32 ONNX | 导出的浮点 ONNX，对照模型 |
| INT8 对照组 | 可量化卷积设为 INT8，Detect/Decode 相关 40 个节点保持 FP32 |
| INT4/INT8 混合 | 在 INT8 对照组基础上，仅把筛选出的低敏感卷积权重设为 INT4 |
| 校准 | 用训练图像估计量化范围；本实验统一使用 128 张训练图像，个别 G2 使用 512 张作对照 |
| 保护检测头 | `--protect-detect`，防止检测输出被量化后出现全零预测 |

> **重要限制**：当前 INT4/INT8 ONNX 在 ONNX Runtime CUDA 上出现大量 Memcpy/QDQ 开销，速度明显慢于 FP32 ONNX。这不是 FPGA 的实际延迟，也不能写成“GPU 加速”。量化的当前价值是模型存储压缩和 FPGA 部署位宽方案探索。

### 5.2 seed=0 的初始量化探索

| 方案 | 文件大小 | 测试 mAP50-95 | 备注 |
|---|---:|---:|---|
| FP32 ONNX | 14.7 MB | 0.587 | 对照 |
| 全 INT8（未保护输出） | — | 0.000 | 输出量化范围问题，废弃 |
| INT8，检测头 FP32 | 4.49 MB | 0.582 | 基础量化对照 |
| 固定 G2 INT4 + 其余 INT8 | 3.35 MB | 0.581 | 相对 INT8 对照额外 -0.001 |
| G2+G6 INT4 + 其余 INT8 | 3.02 MB | 0.580 | 相对 INT8 对照额外 -0.002；seed=0 初始选择 |
| 深层 INT4（6–10） | 3.43 MB | 0.581 | 量化模型可用但 ORT GPU 推理很慢 |

### 5.3 Mixed-QAT 与 PTQ 组合探索

| 方案 | 验证/测试 mAP50-95 | 结论 |
|---|---:|---|
| Mixed-QAT 训练后 FP32 导出测试 | 约 0.580 | 未表现出优于原始 W0.625 的部署精度 |
| Mixed-QAT 后再做深层 INT4/INT8 PTQ | 验证 0.594，测试 0.580 | 未获得稳定优势 |

因此 QAT 目前作为探索性消融保留，不作为最终部署方案。

## 6. 敏感度引导混合精度量化（SG-MPQ）

### 6.1 方法定义

对同一 seed 的已导出 FP32 ONNX，先生成“INT8 对照组”；随后一次只将一个候选层组从 INT8 降为 INT4。定义：

```text
层组额外损失 = mAP50-95(INT8 对照组) - mAP50-95(该层组 INT4)
```

- 损失小：该层组对 INT4 相对低敏感，可作为候选；
- 损失大：该层组高敏感，应保持 INT8/FP32；
- 最终组合需要再次验证，不能把单组结果直接相加。

候选层组宏索引：G2 = `6 7 8 9 10`，G3 = `11 12 13 14`，G6 = `22 23 24`。实际 INT4 Conv 数受导出图结构影响。

### 6.2 seed=0 组敏感度初筛（历史结果）

| 层组 | 验证集 mAP50-95 | 初始判断 |
|---|---:|---|
| G1 | 0.574 | 高敏感，拒绝 |
| G2 | 0.589 | 候选 |
| G3 | 0.590 | 候选 |
| G4 | 0.582 | 高敏感，拒绝 |
| G5 | 0.585 | 高敏感，拒绝 |
| G6 | 0.589 | 候选 |

seed=0 选择 G2+G6，测试结果见第 5.2 节。该阶段的层选择是早期探索，后续 seed=1、seed=2 才引入严格的 INT8 对照组。

### 6.3 seed=1 严格对照

#### 验证集筛选

| 方案 | mAP50-95 | 相对 INT8 对照的额外损失 | 判断 |
|---|---:|---:|---|
| FP32 训练模型（训练末验证） | 0.595 | — | 参考 |
| INT8 对照组 | 0.583 | 0.000 | 基准 |
| G2 INT4 | 0.583 | 0.000 | 低敏感 |
| G3 INT4 | 0.584 | -0.001 | 低敏感 |
| G2+G3 INT4 | 0.582 | 0.001 | 组合通过 |

#### 测试集确认

| 方案 | mAP50-95 | 相对 INT8 对照的额外损失 |
|---|---:|---:|
| FP32 ONNX | 0.588 | — |
| INT8 对照组 | 0.575 | 0.000 |
| G2 INT4 | 0.573 | 0.002 |
| G3 INT4 | 0.575 | 0.000 |
| **G2+G3 INT4** | **0.574** | **0.001** |

seed=1 的自适应选择为 **G2+G3 INT4**（20 个 Conv 权重 INT4）。

### 6.4 seed=2 严格对照

#### 验证集筛选

| 方案 | mAP50-95 | 相对 INT8 对照的额外损失 | 判断 |
|---|---:|---:|---|
| INT8 对照组 | 0.576 | 0.000 | 基准 |
| G2 INT4 | 0.574 | 0.002 | 候选 |
| G3 INT4 | 0.573 | 0.003 | 边界候选 |
| G2+G3 INT4 | 0.573 | 0.003 | 验证集通过，但需测试确认 |

#### 测试集确认

| 方案 | mAP50-95 | 相对 INT8 对照的额外损失 | 结论 |
|---|---:|---:|---|
| FP32 ONNX | 0.590 | — | 导出正确 |
| INT8 对照组 | 0.567 | 0.000 | 基准 |
| G2+G3 INT4 | 0.563 | 0.004 | 组合误差偏大，拒绝 |
| **G2 INT4** | **0.565** | **0.002** | 较稳的补充验证 |

seed=2 中 G3 更接近敏感边界，因此应保守地只保留 **G2 INT4**。

### 6.5 三 seed 总结

| seed | FP32 ONNX 测试 | INT8 对照测试 | 选择的 INT4 层组 | 混合量化测试 | INT8→混合的额外损失 |
|---:|---:|---:|---|---:|---:|
| 0 | 0.587 | 0.582 | G2+G6 | 0.580 | -0.002 |
| 1 | 0.588 | 0.575 | G2+G3 | 0.574 | -0.001 |
| 2 | 0.590 | 0.567 | G2-only | 0.565 | -0.002 |
| 平均 ± 样本标准差 | **0.588 ± 0.002** | **0.575 ± 0.008** | 自适应 | **0.573 ± 0.008** | **-0.002** |

### 6.6 量化结论与写作边界

可以写：

1. 不同层组、不同随机种子的 INT4 敏感度不同，固定层号的一刀切方案不可靠；
2. 在 INT8 对照组基础上，采用敏感度筛选后加入 INT4，额外损失平均约 0.002 mAP50-95；
3. 模型文件在 seed=0 上可由 FP32 ONNX 的 14.7 MB 缩小至约 3.02 MB（G2+G6 示例）；
4. 该结果为后续 FPGA 位宽分配提供候选方案。

不能写：

1. “量化提升检测精度”；
2. “ONNX Runtime GPU 上 INT4 推理更快”；
3. “已验证 FPGA 延迟/资源占用”。

特别说明：seed=2 的 G2-only 测试是在看到 G2+G3 测试偏低后追加的补充消融，因此论文中应标为“补充验证”，不能表述为完全独立的最终模型选择。

## 7. 运行时与数据警告说明

| 现象 | 含义 | 处理 |
|---|---|---|
| `Cache directory ... not writeable: WinError 183` | 标签缓存写入失败/冲突 | 不影响当前扫描、训练或指标；后续可清理旧 cache 或检查目录权限 |
| `NMS time limit exceeded` | 后处理偶尔超过 YOLOv5 时间阈值 | 已输出指标可用；速度统计受影响，不应作为硬件性能结论 |
| ORT `Memcpy nodes are added` | INT4/INT8 QDQ 图有 CPU/GPU 数据传输或缺少高效 CUDA 内核 | 当前 INT4 延迟不可用于比较 GPU/FPGA 加速 |
| 非 PyTorch 强制 batch=1 | ONNX 验证脚本的模型格式限制 | 不影响精度对照，但速度不能与 PT batch=4 直接横比 |

## 8. 归档与版本管理原则

### 应提交 Git 的内容

- `models/` 中新增模型 YAML；
- `models/common.py`、`models/yolo.py`、`train.py`、`export.py` 等源码改动；
- `data/scripts/quantize_onnx_int8.py` 等可复现实验脚本；
- 实验说明 Markdown（若希望版本化）。

### 不应提交 Git 的内容

- `runs/` 目录；
- `.pt` 权重、`.onnx` 模型、缓存、图片结果；
- 大型测试输出和外部归档副本。

外部归档目录：

```text
E:\experiment_M2Y5\04_quantization\sensitivity_guided_mixed_precision
```

## 9. 当前可用于汇报的主结论

1. 以 MobileNetV2 替换 YOLOv5s Backbone，并选择 W0.625，获得 3.685M 参数、10.1 GFLOPs 的轻量模型；相对 YOLOv5s 参数减少约 47.5%，三 seed 平均测试 mAP50-95 为 0.589 ± 0.002。
2. 多个融合模块、注意力、蒸馏、WIoU、稀疏训练和 QAT 均已进行可复现实验；没有稳定提升的方案未被强行纳入最终模型。
3. 建立了 FP32→INT8→选择性 INT4 的对照链；量化层选择必须依赖当前模型的敏感度，而不能跨 seed 固定套用层号。
4. 当前量化结果的定位是“面向 FPGA 的位宽分配与存储压缩探索”，不是 GPU 加速结果；真正的 FPS、功耗、资源占用需在 Jetson 或 FPGA 实物平台完成。

## 10. 后续建议（不新增训练时）

1. 用本文件制作论文/答辩的三张表：宽度消融表、三 seed 复现表、混合量化表；
2. 整理每个主实验目录中的 `results.png`、`confusion_matrix.png`、PR/F1 曲线和预测可视化；
3. 若后续做视频流展示，使用 FP32 `.pt` 或 FP32 ONNX 主模型；不要用当前 ORT INT4 延迟作为实时展示版本；
4. 若未来拥有目标 FPGA，再按其工具链重新量化、综合并实测资源/延迟。
