# 方法章节初稿：面向边缘部署的 MobileNetV2-YOLOv5 安全帽检测与敏感度引导混合量化

> 本文档为论文“方法”章节的中文初稿。文中“本文”对应当前项目；正式定稿时需按目标期刊格式补充引用、编号和结构示意图。

## 3 方法

### 3.1 方法总体框架

施工现场的安全帽佩戴检测通常同时受到遮挡、复杂背景以及边缘端算力与存储资源有限等因素的影响。SHWD 将“佩戴安全帽的头部”标为 `hat` 正类，将“未佩戴安全帽的普通头部”标为 `person` 负类；因此，本文任务的核心是区分佩戴与未佩戴安全帽的头部，而不是检测完整人体框。为在检测精度、模型复杂度和部署存储开销之间取得平衡，本文构建了面向边缘部署的 MobileNetV2-YOLOv5 检测方法，并在模型导出后引入敏感度引导混合精度量化策略。整体流程如图 1 所示：首先以 MobileNetV2 倒残差结构替换 YOLOv5s 原始 Backbone，并通过宽度系数选择轻量化配置；随后保留 YOLOv5 的 FPN、PAN 与三尺度 Detect Head，完成头部安全帽佩戴状态检测；最后，在 ONNX 静态量化阶段以验证集为依据，为不同卷积层组分配 INT4 或 INT8 权重精度，同时保留对检测输出敏感的节点为 FP32。

该方法由两个相互衔接的部分组成：

1. **轻量化检测网络。** 使用 MobileNetV2 Backbone 降低特征提取阶段的参数量和计算量，同时保持 P3、P4、P5 三尺度检测路径，以维持对不同尺度安全帽目标的检测能力。
2. **敏感度引导混合精度量化（SG-MPQ）。** 对已训练模型中的候选卷积层组逐组执行 INT4 敏感度评估，根据验证集精度损失与模型存储收益选择低敏感层组；其他卷积保持 INT8，Detect/Decode 相关节点保留 FP32。

需要说明的是，本文的量化结果用于验证模型存储压缩和后续 FPGA 位宽分配的可行性；在未完成目标硬件上的综合、资源与功耗实测前，本文不将其表述为 FPGA 或 GPU 推理加速结果。

### 3.2 MobileNetV2-YOLOv5 轻量化检测网络

#### 3.2.1 MobileNetV2 倒残差特征提取单元

原始 YOLOv5s Backbone 采用 CSP 风格卷积结构，具有较强表征能力，但在边缘端部署时仍有进一步压缩空间。本文将其替换为由 MobileNetV2Block 构成的轻量化 Backbone。每个 MobileNetV2Block 依次包括逐点扩张卷积、深度可分离卷积和逐点投影卷积；当输入、输出通道数相同且步长为 1 时，引入残差连接。

给定输入特征 $\mathbf{X}\in\mathbb{R}^{H\times W\times C}$，扩张倍率为 $t$，倒残差单元可写为：

$$
\mathbf{Z}=\mathcal{P}_{1\times1}\left(\mathcal{D}_{3\times3}\left(\sigma\left(\mathcal{E}_{1\times1}(\mathbf{X})\right)\right)\right),
$$

其中，$\mathcal{E}_{1\times1}(\cdot)$ 表示将通道扩张至 $tC$ 的 $1\times1$ 卷积，$\mathcal{D}_{3\times3}(\cdot)$ 表示按通道独立计算的 $3\times3$ 深度卷积，$\mathcal{P}_{1\times1}(\cdot)$ 表示线性投影卷积，$\sigma(\cdot)$ 为 ReLU6 激活函数。当空间尺寸与通道数均匹配时，输出为：

$$
\mathbf{Y}=\mathbf{X}+\mathbf{Z};
$$

否则直接令 $\mathbf{Y}=\mathbf{Z}$。与常规卷积相比，深度卷积将空间卷积和通道混合解耦，可有效减少计算量；残差连接则有利于浅层特征传播和网络优化。

#### 3.2.2 宽度系数与多尺度检测结构

为在精度和复杂度之间建立可控的折中，本文采用宽度系数 $\alpha$ 对网络通道数进行统一缩放：

$$
C^{\prime}=\operatorname{make\_divisible}(\alpha C),
$$

其中，$C$ 为基础通道数，$C^{\prime}$ 为缩放后的通道数。本文对 $\alpha=0.50$、$0.625$ 与 $0.75$ 进行了宽度消融，并选择 $\alpha=0.625$ 作为最终主模型配置。该配置在保持三尺度检测能力的同时，避免 $\alpha=0.75$ 接近原始 YOLOv5s 的计算量，也缓解了 $\alpha=0.50$ 的精度下降。

轻量化 Backbone 在下采样倍率为 8、16 和 32 的位置分别输出 P3、P4 和 P5 特征。之后，保留 YOLOv5 的 FPN 与 PAN 融合路径：FPN 自顶向下传递高层语义信息，PAN 自底向上传递定位信息，最终在 P3、P4、P5 上分别设置 Detect Head。P3 分支承担较小头部目标的主要检测任务，P4 与 P5 分支用于补充中、大尺度头部信息。保留该三尺度结构可避免因过度轻量化而直接削弱小目标检测路径。

### 3.3 敏感度引导混合精度量化

#### 3.3.1 量化基线与输出保护

训练完成后，首先将 PyTorch 权重导出为 FP32 ONNX 模型，作为部署精度参考。随后采用静态后训练量化（PTQ）建立 INT8 对照模型：候选卷积层的激活使用 UINT8 表示，权重使用按输出通道的 INT8 对称量化；量化范围由 128 张训练图像通过 MinMax 校准获得。

初步实验发现，若将 Detect Head 与后续 Decode 节点一并量化，模型会出现检测输出退化。因此，本文将 Detect Head 与 Decode 相关节点从量化图中排除，始终保持 FP32。该处理使量化重点集中于 Backbone 和 Neck 的卷积计算与权重存储，同时避免直接破坏边界框、置信度和类别输出。

对于待量化张量 $x$，均匀量化可抽象表示为：

$$
q=\operatorname{clip}\left(\operatorname{round}\left(\frac{x}{s}\right)+z, q_{\min},q_{\max}\right),
$$

其中，$s$ 为尺度因子，$z$ 为零点，$q$ 为低比特整数表示。推理时根据相应量化参数执行反量化或整数算子计算。本文使用 QDQ 图格式保存量化与反量化关系，以保持 ONNX 图的可追溯性。

#### 3.3.2 层组敏感度评估

不同卷积层对权重量化的敏感程度并不一致。若直接将全部层统一降为 INT4，可能造成检测头前关键特征表达受损；若所有层均保持 INT8，则无法充分降低权重存储开销。为此，本文将 Backbone 与 Neck 中的候选卷积按网络宏层划分为若干组，记为 $\mathcal{G}=\{g_1,g_2,\ldots,g_K\}$。

对每一个候选层组 $g_k$，在保持其余可量化卷积为 INT8、Detect/Decode 节点为 FP32 的条件下，仅将 $g_k$ 对应卷积权重设为按输出通道量化的 INT4。令 $m_{\mathrm{INT8}}$ 为 INT8 对照模型在验证集上的 mAP50--95，$m_{g_k}^{\mathrm{INT4}}$ 为仅将层组 $g_k$ 降为 INT4 后的验证集 mAP50--95，则该层组的额外精度损失定义为：

$$
\Delta_k=m_{\mathrm{INT8}}-m_{g_k}^{\mathrm{INT4}}.
$$

$\Delta_k$ 越小，说明该层组对 INT4 权重量化越不敏感；在模型存储收益相近时，优先选择 $\Delta_k$ 更小的层组。对于由多个候选层组组成的组合 $\mathcal{S}\subseteq\mathcal{G}$，需要重新进行验证，而不能简单将单组损失相加：

$$
\mathcal{S}^{*}=\arg\min_{\mathcal{S}}\ \Delta(\mathcal{S})\quad
\text{s.t.}\quad \operatorname{Size}(\mathcal{S})\leq B,
$$

其中，$B$ 表示给定的模型存储预算，$\Delta(\mathcal{S})$ 表示该组合相对于 INT8 对照的验证集精度损失。实际选择时同时考察验证集精度与模型文件大小，并对候选组合执行独立验证。

#### 3.3.3 模型特异性的位宽分配

SG-MPQ 的关键不在于预设一套固定层号，而在于对**当前已训练模型**进行验证集驱动的位宽分配。不同随机种子训练得到的权重分布不同，相同宏层在 INT4 下的敏感度也可能变化。因此，每个部署候选模型均遵循“FP32 ONNX → INT8 对照 → 单组敏感度评估 → 候选组合验证”的流程，最终得到模型特异性的 INT4/INT8 配置。

该策略有两点优势：其一，避免使用跨模型固定的经验层号而造成不可控精度下降；其二，在不重新训练模型的前提下，为 FPGA 后续的片外存储、带宽与定点乘加位宽设计提供可量化的候选配置。需要强调，本文当前验证的是位宽分配与模型压缩效果；实际 FPGA 加速效率还取决于目标器件、编译器、片上缓存与算子映射，应在硬件部署阶段单独测量。

### 3.4 方法流程总结

本文方法可概括为以下步骤：

1. 使用 MobileNetV2 倒残差块构建轻量化 Backbone，并保留 YOLOv5 的 P3、P4、P5 多尺度检测结构；
2. 通过宽度系数消融选择 W0.625 作为精度—复杂度折中模型；
3. 将训练完成的模型导出为 FP32 ONNX，并建立保护 Detect/Decode 节点的 INT8 对照模型；
4. 在验证集上逐组评估候选卷积层的 INT4 敏感度，选择低敏感层组并验证组合效果；
5. 输出模型特异性的混合 INT4/INT8 部署候选，为后续 FPGA 实现提供位宽分配依据。

## 待补充的图与引用

- **图 1：总体框架图。** 输入图像 → MobileNetV2 Backbone → FPN/PAN → P3/P4/P5 Detect → FP32 ONNX → INT8 对照 → 层组敏感度筛选 → 混合 INT4/INT8 ONNX。
- **图 2：MobileNetV2Block。** $1\times1$ 扩张 → $3\times3$ 深度卷积 → $1\times1$ 投影；满足条件时添加残差支路。
- **图 3：SG-MPQ 筛选流程。** 单组 INT4 试验、验证集计算 $\Delta_k$、候选组合验证与模型导出。
- **参考文献占位。** MobileNetV2 原始论文；YOLOv5 官方实现/相关文献；静态量化或整数量化基础文献；安全帽检测相关工作。
