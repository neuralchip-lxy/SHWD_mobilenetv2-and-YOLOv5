# 论文实验章节材料（初稿）

> 数据与结果依据：`分支实验全量对照汇总.md`、`新对话交接说明_2026-09-04.md`。
>
> 使用边界：本文档仅整理已经完成的 SHWD 实验；不将 ONNX Runtime CUDA 上的 INT4 延迟作为 GPU 或 FPGA 加速结果。

## 1. 实验设置（可直接写入论文）

实验在 SHWD 安全帽佩戴检测数据集上开展：`hat` 表示佩戴安全帽的头部正类，`person` 表示未佩戴安全帽的普通头部负类。训练、验证和测试集分别包含 5457、607 和 1517 张图像；输入分辨率统一为 640 × 640。除特别说明外，模型使用 SGD 优化器训练 200 个 epoch，batch size 为 8，随机种子设置为 0、1、2。模型及量化层组仅依据验证集筛选，测试集用于最终结果报告。

本文以 mAP50--95 为主要精度指标，同时报告 mAP50、Precision 和 Recall。参数量与 GFLOPs 用于衡量模型复杂度。对于量化部署探索，FP32 ONNX、保护 Detect/Decode 节点的 INT8 ONNX 及混合 INT4/INT8 ONNX 使用相同的 128 张训练图像进行校准。

## 2. 宽度系数消融：主模型选择

表 1 的宽度消融均为 seed=0 结果，用于选择精度—复杂度折中模型；三随机种子统计见表 2。

| 模型 | 参数量 (M) | GFLOPs | 验证集 mAP50--95 | 测试集 mAP50--95 |
|---|---:|---:|---:|---:|
| YOLOv5s | 7.016 | 15.8 | 0.609 | 0.601 |
| MobileNetV2-YOLOv5 W0.50 | 2.369 | 6.4 | 0.588 | 0.578 |
| **MobileNetV2-YOLOv5 W0.625** | **3.685** | **10.1** | **0.599** | **0.588** |
| MobileNetV2-YOLOv5 W0.75 | 5.283 | 14.0 | 0.602 | 0.596 |

相较于 YOLOv5s，W0.625 的参数量和 GFLOPs 分别降低 47.5% 和 36.1%，测试集 mAP50--95 仅降低 0.013。W0.50 虽然更轻，但精度下降明显；W0.75 的精度略高，但计算量已接近 YOLOv5s。因此，后续实验选取 W0.625 作为主轻量化模型。

## 3. 三随机种子复现

| seed | Precision | Recall | mAP50 | mAP50--95 |
|---:|---:|---:|---:|---:|
| 0 | 0.921 | 0.882 | 0.932 | 0.588 |
| 1 | 0.919 | 0.880 | 0.931 | 0.588 |
| 2 | 0.923 | 0.875 | 0.931 | 0.591 |
| **平均 ± 样本标准差** | — | — | **0.931 ± 0.001** | **0.589 ± 0.002** |

三组实验的结果波动很小，说明所选 W0.625 结构在固定数据划分、训练超参数和输入尺寸下具有较好的稳定性。论文主精度结论应采用 **mAP50--95 = 0.589 ± 0.002**，而不是只报告 seed=0 的单次结果。

## 4. 结构与训练策略消融

| 方案 | 验证集 mAP50--95 | 处理与结论 |
|---|---:|---|
| W0.625 原模型 | 0.599 | 参照模型 |
| SGCF 语义引导跨尺度融合 | 0.597 | 未提升，不采用 |
| Plain P4→P3 Fusion | 0.601 | 仅 +0.002，接近随机波动，不作为核心方法 |
| RGCF 残差门控跨尺度融合 | 0.599 | 无稳定收益，不采用 |
| QCA 坐标注意力 | 0.592 | 精度下降，不采用 |
| 普通知识蒸馏 KD | 0.599 | 无稳定收益，不采用 |
| SCCD 蒸馏 | 0.598 | 无稳定收益，不采用 |
| WIoU（50 epoch） | 0.540 | 明显下降，停止 |
| BN 稀疏正则（50 epoch） | 0.599 | 尚未删除通道，不能表述为结构化剪枝 |
| Mixed-QAT | 训练验证 0.601 | 部署精度无稳定优势，不采用 |

该表应以“消融与方案筛选”呈现：本文没有将未带来稳定收益的模块强行叠加到最终模型，而是保留 MobileNetV2-W0.625 的简洁结构，以兼顾轻量化与后续硬件部署可行性。

## 5. 敏感度引导混合精度量化

量化时，Detect/Decode 相关节点保持 FP32；其余可量化卷积首先构成 INT8 对照，再根据验证集上的敏感度将低敏感组的卷积权重降为 INT4。层组额外损失定义为：

```text
ΔmAP = mAP50--95(INT8 对照组) − mAP50--95(混合 INT4/INT8 组)
```

| seed | FP32 ONNX 测试 | INT8 测试 | 验证集筛选的 INT4 层组 | 混合量化测试 | INT8→混合额外损失 |
|---:|---:|---:|---|---:|---:|
| 0 | 0.587 | 0.582 | G2+G6 | 0.580 | 0.002 |
| 1 | 0.588 | 0.575 | G2+G3 | 0.574 | 0.001 |
| 2 | 0.590 | 0.567 | G2-only | 0.565 | 0.002 |
| **平均 ± 样本标准差** | **0.588 ± 0.002** | **0.575 ± 0.008** | 模型自适应 | **0.573 ± 0.008** | **0.002** |

在 seed=0 上，G2+G6 示例将 ONNX 文件由 14.7 MB 压缩至 3.02 MB。三随机种子实验表明，低敏感层组会随训练得到的权重而变化；因此，论文应强调“基于验证集的模型特异性敏感度筛选”，而非声称固定层号适用于所有模型。

注意：seed=2 的 G2-only 测试是在观察到 G2+G3 测试结果偏低后追加，论文中应标注为补充消融验证，不应将其表述为完全独立的最终选择。

## 6. 可使用的主结论

1. 使用 MobileNetV2 替换 YOLOv5s Backbone 并选择宽度系数 0.625，可在有限精度损失下显著降低模型参数量和计算量。
2. W0.625 在三个随机种子上的测试 mAP50--95 为 0.589 ± 0.002，验证了结论的稳定性。
3. 层敏感度引导的混合 INT4/INT8 量化可在 INT8 对照基础上仅带来约 0.002 mAP50--95 的平均额外损失，并提供显著的模型存储压缩候选。
4. 当前量化实验是面向 FPGA 的位宽分配与存储压缩探索；尚未获得 FPGA 资源、功耗或端到端延迟实测，不能宣称硬件加速。

## 7. 图件选择清单

论文正文建议优先使用 seed=0 主模型的下列图件，避免同类图过多：

| 图号建议 | 内容 | 推荐源文件 |
|---|---|---|
| 图 1 | W0.625 训练收敛曲线 | `yolov5-master/runs/paper/mobilenetv2_w0625_s0/results.png` |
| 图 2 | W0.625 测试集 PR 曲线 | `yolov5-master/runs/paper_val/mobilenetv2_w0625_s0_test_clean/PR_curve.png` |
| 图 3 | W0.625 测试集混淆矩阵 | `yolov5-master/runs/paper_val/mobilenetv2_w0625_s0_test_clean/confusion_matrix.png` |
| 图 4 | 正确检测与挑战案例 | 从 `.../mobilenetv2_w0625_s0_test_clean/val_batch*_pred.jpg` 裁取并标注典型区域 |

不建议直接把多个 seed 的 PR 曲线、所有失败模块的曲线或 ONNX Runtime INT4 的延迟图放入正文；它们可保留在补充材料或答辩备份页。

## 8. 下一项文稿工作

在确认上述表格数字无误后，下一步起草论文的“方法”与“实验设置”两节：明确 MobileNetV2-YOLOv5 的结构替换方式、宽度系数选择逻辑、SG-MPQ 的验证集敏感度筛选流程及其适用边界。
